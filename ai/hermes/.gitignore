# Runtime/máquina — no versionar (se materializa en Application Support)
.venv/
__pycache__/
*.pyc
*.log
# Estado mutable de Hermes (va a Application Support en el .app)
data/
sessions/
*.db
response_store.db
hermes-runtime/
node/
.agent-browser/
# Secretos / config de runtime (NUNCA al repo público)
.env
secrets/
config.yaml
api_server_key
